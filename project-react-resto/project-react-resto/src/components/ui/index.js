/**
 * UI Components Index
 * 
 * This file exports all UI components from a single entry point,
 * making it easier to import them in other components.
 */

import Alert from './Alert';
import Button from './Button';
import Card from './Card';
import Modal, { ConfirmationModal } from './Modal';
import Table from './Table';

// Export all components
export {
  Alert,
  Button,
  Card,
  Modal,
  ConfirmationModal,
  Table
};

// Export a default object with all components
export default {
  Alert,
  Button,
  Card,
  Modal,
  ConfirmationModal,
  Table
};
